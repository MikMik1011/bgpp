<h1>work in progress</h1>
<h2>api v2 only for now!</h2>
<ul>
    <li><code>/api/v2/[city]/stations</code> – get all stations in the city</li>
    <li><code>/api/v2/[city]/stations/hash</code> – get hash value for all stations response</li>
    <li><code>/api/v2/[city]/stations/[stationId]/arrivals</code> – get arrivals for that station</li>
    <li><code>/api/v2/cities</code> – get list of all supported cities</li>
</ul>